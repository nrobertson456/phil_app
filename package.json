{
  "name": "phil_app",
  "version": "1.0.0",
  "private": true,
  "description": "Curtain Call — Broadway musical writing app (backend + frontend)",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}

